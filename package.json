{
  "name": "prisma-bug",
  "version": "1.0.0",
  "scripts": {
    "prisma generate": "npx prisma generate",
    "prisma migrate": "npx prisma migrate dev",
    "build:run": "npm ci && npx prisma generate && npx prisma migrate dev -n init && npx ts-node app.ts"
  },
  "dependencies": {
    "@prisma/client": "^2.20.1",
    "express": "4.17.1"
  },
  "devDependencies": {
    "@types/express": "^4.17.11",
    "@types/node": "^14.14.37",
    "prisma": "^2.20.1",
    "ts-node": "^9.1.1",
    "typescript": "^4.2.3"
  }
}
